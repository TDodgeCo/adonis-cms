@layout('main')
@section('content')
  <h3>Please take a moment and create a safe, memorable password for future access to your account.</h3>
  <br>
  <form id="postForm" action="{{'/set-password/?_method=PUT'}}" method="POST">
    {{ csrfField() }}
    <div class="form-group">
      <label>Password</label>
      <input type="password" id="password" name="password" class="form-control" placeholder="Password">
      {{ elIf('<span class="text-danger">$self</span>', getErrorFor('password'), hasErrorFor('password')) }}
    </div>
    <div class="form-group">
      <label>Confirm Password</label>
      <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" placeholder="Password">
      {{ elIf('<span class="text-danger">$self</span>', getErrorFor('password'), hasErrorFor('password')) }}
    </div>
    <button id="signupButton" type="submit" class="btn btn-primary">Submit</button>
  </form>
  {{ script('./js/auth-helpers.js')}}
@endsection
